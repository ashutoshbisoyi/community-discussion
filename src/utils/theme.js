import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#7E34F6',
      dark: '#551daf',
      contrastText: '#ffffff',
    },
    secondary: {
      main: '#6b778f',
    },
  },
});

export default theme;
